"""Async autodiscover orchestration (scan, apply, connectors, auto-sync).

HTTP adapters live in ``services/async_read/autodiscover.py`` and
``services/async_write/autodiscover.py``. Pure discovery logic lives in
``services/autodiscover/discovery/`` and ``services/autodiscover/connectors/``.
"""
